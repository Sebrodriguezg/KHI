#!/usr/bin/env bash
set -eu # strict config (non-zero returns and unset vars halt script)

line(){
    echo "==============================================================================="
}

compile_texdoc(){
    tex_filename="$1"
    line
    echo " Compiling ${tex_filename}.tex"
    line
    xelatex ${tex_filename}.tex
#    bibtex   ${tex_filename}
    xelatex ${tex_filename}.tex
    xelatex ${tex_filename}.tex
    line
}

#    option="${2:-}"
    #if [[ "$option" == "update_plots" ]]; then
		# echo "- Updating plots"
        #./main_update_plots.sh
    #fi

line
echo "- Compiling"
#compile_texdoc "presentation_slidesAndNotes_twoscreens" #> /dev/null
compile_texdoc "presentation_slides" # > /dev/null
#compile_texdoc "presentation_notes" > /dev/null || true
#compile_texdoc "presentation_slidesAndNotes" #> /dev/null

line
echo "- Cleaning"
find . -name "*.aux" -delete
find . -name "*.out" -delete
find . -name "*.log" -delete
find . -name "*.toc" -delete
find . -name "*.nav" -delete
find . -name "*.snm" -delete
find . -name "*.bbl" -delete
find . -name "*.blg" -delete

line
echo "- Success, have a good day¡"
line
