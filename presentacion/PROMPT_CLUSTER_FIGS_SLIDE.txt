PROMPT PARA EL CLAUDE DEL CLÚSTER — Figuras "versión slide" para la sustentación
(copiar y pegar todo este bloque tal cual)

================================================================================

Necesito regenerar algunas figuras de la monografía KHI-RRMHD en "versión
slide" para la presentación de sustentación (proyector, media lámina). Antes
de generar nada, BUSCA en este clúster los scripts que crearon las figuras
originales y repórtame dónde están y cómo se corren.

PARTE 1 — Localiza los scripts generadores de estas figuras (busca por nombre
de archivo de salida dentro de los .py, no solo por nombre de script):

  1. fig_conductividad_rho_collage.pdf   (collage 3×3 de ρ, barrido de σ,
     estilo dark de AVP.py, generado aquí ~2026-07-01)
  2. rho_inicial.pdf y velocidad_inicial.pdf   (condición inicial ρ(x,y) y
     v(x,y) del run base, estilo dark de AVP.py, mismos días)
  3. fig_campB_ekin_emag_scatter.pdf   (scatter 2×3 ó 3×2 por ángulo θ de la
     parte oscilatoria E'_kin vs E'_mag, campaña B, con r por panel; puede
     haberse generado aquí ~2026-06-26)

Para cada uno dime: ruta del script, con qué python/venv se corre, y de qué
directorio de datos lee. Si el del punto 3 existe, repórtame EXACTAMENTE cómo
calcula la parte oscilatoria (suavizado/detrending) y los r de cada panel.

PARTE 2 — Genera las versiones slide (sufijo _slide en el nombre, PDF + PNG):

REGLA GENERAL: fuentes de ejes/leyendas ≥14–16 pt en el canvas, SIN título
interno (el título de la lámina ya lo dice), y solo los paneles necesarios.
Mantén el estilo dark nativo de AVP.py donde aplique (fondo negro, inferno,
contornos blancos finos, fuente serif, texto blanco, DPI ≥ 220).

  A. fig_conductividad_rho_collage_slide.pdf
     - El mismo collage 3×3 (filas = 3 conductividades: resistiva, transición
       ≈2815, ideal; columnas = 3 tiempos hasta t≈15) con los MISMOS runs y
       frames que la versión de la monografía.
     - SIN título interno. SIN ticks ni números en los ejes (es cualitativa).
     - Rótulos de fila (σ=…) y de columna (t=…) GRANDES (≥16 pt).
     - Una sola colorbar fina sin borde, etiqueta ρ, con solo 3–4 ticks
       grandes.

  B. setup_inicial_slide.pdf  (reemplaza el par rho_inicial + velocidad_inicial)
     - UN solo PDF con 2 paneles lado a lado: ρ(x,y) inicial y v(x,y) inicial
       del run base (los mismos datos/frame que rho_inicial.pdf y
       velocidad_inicial.pdf de la monografía).
     - SIN títulos internos; ejes x, y con fuentes grandes (≥14 pt) — aquí SÍ
       van ticks porque la lámina presenta el dominio; colorbar por panel o
       compartida si aplica, con pocos ticks grandes.

Cuando termines: repórtame qué runs/frames usaste y deja los PDF en una ruta
que me puedas indicar para descargarlos (o dime cómo los recojo).
================================================================================
